services:
  - type: web
    name: routewriter
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GOOGLE_MAPS_API_KEY
        sync: false
